import { AppbuttondisableDirective } from './appbuttondisable.directive';

describe('AppbuttondisableDirective', () => {
  it('should create an instance', () => {
    const directive = new AppbuttondisableDirective();
    expect(directive).toBeTruthy();
  });
});
