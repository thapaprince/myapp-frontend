import { DynamichoverDirective } from './dynamichover.directive';

describe('DynamichoverDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamichoverDirective();
    expect(directive).toBeTruthy();
  });
});
