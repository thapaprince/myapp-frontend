import { AppifDirective } from './appif.directive';

describe('AppifDirective', () => {
  it('should create an instance', () => {
    const directive = new AppifDirective();
    expect(directive).toBeTruthy();
  });
});
