import { AppclickkeyDirective } from './appclickkey.directive';

describe('AppclickkeyDirective', () => {
  it('should create an instance', () => {
    const directive = new AppclickkeyDirective();
    expect(directive).toBeTruthy();
  });
});
